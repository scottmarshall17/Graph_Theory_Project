# Author: Scott Marshall
# Date: 10 December 2014

#This program takes three arguments from the command line, srcID, destID, and fileName. 
#The program reads the data from the file and creates a graph data structure from the
#parameters within the file. It then finds the shortest and most reliable paths from the srcId
#to the destID and prints them to the screen.